[build]
  publish = "frontend-vanilla"
  command = "npm run build:frontend"

# Optional: if you prefer Netlify to proxy /api to your backend (no CORS required),
# uncomment and set the target backend below.
[[redirects]]
#  from = "/api/*"
#  to = "https://api.your-backend.com/:splat"
#  status = 200
#  force = true
